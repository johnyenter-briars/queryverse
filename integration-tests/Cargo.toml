[package]
name = "queryverse-integration-tests"
version = "0.1.0"
edition = "2021"

[dependencies]
queryverse_lib = { path = "../src-tauri", package = "QueryVerse" }
powerplatform-dataverse-client = "0.9.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }
