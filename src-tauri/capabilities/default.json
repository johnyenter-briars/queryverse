{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["QueryVerse"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
